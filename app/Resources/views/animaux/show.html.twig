{% extends 'base.html.twig' %}

{% block body %}

    <div class="container">
        <div class="row">
            <div class="col-xs-12 col-sm-12 col-md-6 col-lg-6 col-xs-offset-0 col-sm-offset-0 col-md-offset-3 col-lg-offset-3 toppad" >
                <div class="panel panel-info">
                    <div class="panel-heading">
                        <h3 class="panel-title">{{ animaux.name }}</h3>
                    </div>
                    <div class="panel-body">
                        <div class="row">
                            <div class="col-md-3 col-lg-3 " align="center"> <img alt="User Pic" src="{{ asset('uploads/images/' ~ animaux.photo) }}" class="img-circle img-responsive"> </div>
                            <div class=" col-md-9 col-lg-9 ">
                                <table class="table table-user-information">
                                    <tbody>
                                    <tr>
                                        <td>ID:</td>
                                        <td>{{ animaux.id }}</td>
                                    </tr>
                                    <tr>
                                        <td>Type:</td>
                                        <td>{{ animaux.type }}</td>
                                    </tr>
                                    <tr>
                                        <td>Sexe:</td>
                                        <td>{{ animaux.sexe }}</td>
                                    </tr>
                                    <tr>
                                    <tr>
                                        <td>Propriétaire:</td>
                                        <td>{{ animaux.user }}</td>
                                    </tr>
                                    </tr>
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                    <div class="panel-footer">
                        <span class="pull-right">
                             <a href="{{ path('animaux_edit', { 'id': animaux.id }) }}" class="btn btn-info"><em class="fa fa-edit"></em></a>
                        </span>
                        <br/>
                        <br/>
                    </div>
                </div>
                {{ form_start(delete_form) }}
                <button type="submit" class="btn btn-danger"><em class="fa fa-delete" value="Delete"></em></input>
                    {{ form_end(delete_form) }}
            </div>
        </div>
    </div>


{% endblock %}
